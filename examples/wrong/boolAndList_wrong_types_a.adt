
adt List
sorts List, T
ops
    Nil:                -> List
    cons: T x List -> List
    concat: List x List -> List
    head: List          -> T
    tail: List          -> List
axs
    concat(Nil, x) = x
    concat(cons(e, x), y) = e
    head(cons(e,x)) = e
    tail(cons(e,x)) = x
    tail(Nil) = Nil
end